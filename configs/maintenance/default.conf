server {
    listen       80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # Redirect all requests (e.g. /admin/, /login) to index.html
        try_files $uri $uri/ /index.html;
    }
}